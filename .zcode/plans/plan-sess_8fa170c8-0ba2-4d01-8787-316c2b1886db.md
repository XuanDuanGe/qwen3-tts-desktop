已定位到根目录 `samples/`，将按其现有暗色界面体系迁移，而不复制其无关页面实现：

1. 把 `samples/tailwind.config.js` 的颜色 token 和 `rounded-ui` 圆角原样迁入 `desktop/tailwind.config.js` 的 `theme.extend`，保留当前 Electron 渲染进程扫描路径。
2. 基于 samples 的窗口栏视觉更新现有 `TitleBar`：使用 canvas 背景、底部边框、主色调品牌标记、紧凑字体与控制区；保留当前项目已实现的无边框窗口 IPC、拖拽区和三个窗口控制按钮。交互按钮继续明确设为 `no-drag`，不会被拖拽层遮挡。
3. 新建通用 `Sidebar` 组件，参考 samples 的 canvas 背景、分隔边框、激活/悬浮状态和图标样式，导航项严格使用当前项目已有路径与文案：`首页`、`语音生成`、`语音克隆`、`设置`。
4. 将 `AppRoutes` 重组为持续显示的应用壳：顶部窗口栏，下方左侧导航与右侧可滚动内容区；使用 `NavLink` 自动高亮当前路由。四个页面会清空现有临时标题/问候内容，保持空白，符合本次要求。
5. 补充最小全局布局样式（全高、颜色、字体、内容滚动），避免标题栏、侧边栏或内容层相互覆盖。
6. 执行格式化、ESLint 和静态源码检查；按你的要求不运行 `pnpm dev` 或构建。

不引入新依赖，不调整路由路径、主进程或 IPC 功能，也不创建额外页面功能。